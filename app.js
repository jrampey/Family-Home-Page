(async()=>{const child={name:'Ava',birthdate:'2018-11-07T12:00:00'},now=new Date(),start=new Date(now.getFullYear(),0,0),day=Math.floor((now-start)/86400000),birth=new Date(child.birthdate);let age=now.getFullYear()-birth.getFullYear();if(now.getMonth()<birth.getMonth()||(now.getMonth()===birth.getMonth()&&now.getDate()<birth.getDate()))age--;let all={};try{all=await fetch('daily-data.json',{cache:'no-store'}).then(r=>r.json())}catch{}const data=all[String(day)]||{};const scripture=data.scripture||`The Bible Recap — Day ${day}`;dayPill.textContent=`Day ${day} of 365`;ageLabel.textContent=`Written for ${child.name}, age ${age}`;todayLabel.textContent=`${now.toLocaleDateString(undefined,{weekday:'long',month:'long',day:'numeric'})} • A daily recap made for ${child.name}.`;scriptureTitle.textContent=scripture;summaryTitle.textContent=data.title||'Today’s Bible reading';summaryBody.innerHTML=(data.summary||['Open today’s reading and recap using the links above.']).map(x=>`<p>${x}</p>`).join('');takeaway.textContent=data.takeaway||`For ${child.name}: What does today’s reading show you about God?`;bibleLink.href=data.bibleUrl||`https://www.bible.com/reading-plans/42399-the-bible-recap-with-tara-leigh-cobble/day/${day}`;youtubeLink.href=`https://www.youtube.com/results?search_query=${encodeURIComponent('The Bible Recap Day '+day+' '+scripture)}`;})();